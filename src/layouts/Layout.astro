<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Michael | Portfolio</title>
		<meta
			name="description"
			content="Minimal portfolio for Michael — software developer and photographer."
		/>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link
			href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&family=DM+Sans:wght@400;500;600;700&display=swap"
			rel="stylesheet"
		/>
		<style>
			:root {
				--ink: #f8fafc;
				--muted: #cbd5f5;
				--paper: #0a0b0d;
				--line: rgba(248, 250, 252, 0.18);
				--accent: #ffffff;
				--highlight: #e2e8f0;
			}

			* {
				box-sizing: border-box;
			}

			body {
				margin: 0;
				width: 100%;
				min-height: 100vh;
				background: radial-gradient(circle at 15% 18%, rgba(255, 255, 255, 0.08), transparent 46%),
					radial-gradient(circle at 85% 12%, rgba(148, 163, 184, 0.18), transparent 42%),
					radial-gradient(circle at 60% 85%, rgba(30, 41, 59, 0.6), transparent 55%),
					linear-gradient(140deg, #0a0b0d 0%, #0f1117 35%, #0b0c10 100%);
				color: var(--ink);
				font-family: 'Space Grotesk', 'DM Sans', 'Inter', -apple-system, BlinkMacSystemFont,
					'Segoe UI', sans-serif;
				line-height: 1.6;
			}

			a {
				color: inherit;
			}
		</style>
	</head>
	<body>
		<slot />
	</body>
</html>
